# フェーズ 14 — 残り 10 フィールドを作る（屋外のみ）（作業記録）

2026-09-06。素材はプレースホルダのまま、配置と地形だけ。屋内は着手しない。

## T-1 テクスチャのちらつき（俯角 30°）

計測: F05 の通り（(8.5,20)、昼）で注視点を北へ 0.05 m/フレーム、60 フレーム。隣接フレームを整数シフト（±3 px）で位置合わせしてから残差の MAD を取る
（パンの流れを除き、標本化の揺れだけを見る）。屋根だけの MAD は同じパンを面積マスクで撮って屋根画素に限定した。
`field_scene.gd` の `fseq=<dir> fseq_frames=60 pan_m=0.05 wfilter=… roof_contrast=…`。GIF は `docs/lookdev/video/t1_*_x1.gif` / `_x3.gif`（Git 管理外）。

| 条件 | 屋根の MAD（平均 / 最大） | 全画面の MAD |
| --- | --- | --- |
| A Nearest + ミップマップ（現行） | 14.05 / 17.11 | 7.97 |
| Nearest + ミップ + 異方性 16x | 14.14 / 17.16 | 8.95 |
| 線形 + ミップマップ | 11.96 / 14.76 | 6.31 |
| **B 線形 + ミップ + 異方性 16x**（採用） | 12.17 / 15.00 | 7.09 |
| C B + 瓦の明暗 0.5 | 8.95 / 12.87 | 6.81 |
| （参考）B + DOF | 4.00 / 6.28 | 4.18 |
| （参考）Nearest + 異方性 + DOF | 4.33 / 6.68 | 5.42 |

**T-1a: DOF の 2 行は参考であり、採用判断の根拠にしない。** DOF はちらつきを消しているのではなく、ぼかして見えなくしているだけ
（フェーズ 4 E-0a と同じ罠）。ちらつきの測定は DOF OFF で行う（ART_SPEC 第 3 節・第 8 節に明記）。

分かったこと:

- **異方性フィルタの効果はほぼ無い**（Nearest 14.05 → 14.14、線形 11.96 → 12.17）。俯角 30° の屋根は縮小ではなく「1 texel ≒ 1 px のまま斜めに流れる」状態で、
  ミップ選択の問題ではなく、8 texel 周期の瓦の明暗そのものが 1 px 動くたびに差分を出している。
- 線形フィルタは 13 % 減らす（テクセルの縁が半 px 滲む代わり）。**瓦の明暗を半分にすると 37 % 減**。
- 設定は依頼どおり線形 + 異方性 16x を既定にした（`PixelTextures.world_filter`、`project.godot`）。異方性は地形の法面・石段（斜面）で効く見込みなので残す。
  瓦の明暗（`roof_contrast`）は既定 1.0 のまま。0.5〜0.7 にするかは素材の工程で決める。
- 材質の漏れ: 世界材質は 42 個すべて `PixelTextures.register()` を通り、フィルタを一括で切り替えられる（`FIELD ... world_mats=42 filter=5`）。
  暖かい窓（手作り材質）が Nearest のまま漏れていたので登録した。スプライト（ビルボード・主人公）は Nearest のまま。
- DOF は原因ではない。ON で MAD が下がる（−72 %）のは隠しているだけなので、評価から外した（T-1a）。
- T-1b: 分かったのはフィルタ設定ではなくテクスチャ設計の問題。「周期の明暗差を抑える」「不規則な細かさで情報量を出す」を ART_SPEC 第 3 節の素材の規則に追記した。

`t1_roof_frames.png`: 同じフレームの屋根の切り出し（A / B / C、x3）。

## T-2a 高さの形式（承認済み。登れる段差は 1 階調に変更）

`docs/FIELD_FORMAT.md` 第 8 節。要点:

| 項目 | 値 |
| --- | --- |
| ファイル | `data/fields/<ID>_height.png`（任意。無ければ全面 0 m） |
| 解像度 | 1 px = 1 タイル（マスクと同じ）。段や坂の面はタイルの中で生成器が刻む |
| 値 | 8 bit グレー、厳密（R = G = B、A 255）。0 = 基準面、**1 階調 = 0.15 m（石段 1 段）**、255 = 38.25 m。負の高さ無し |
| 段差 | 既定は平らなタイル + 垂直の面。**0〜2 階調（0.30 m）は登れる、3 階調以上は登れない** |
| 坂 | JSON `terrain` の `slope` 矩形。中心の高さを線形補間、差に関わらず渡れる |
| 階段 | `stairs` 矩形。1 タイルに（差）段の蹴上げ 0.15 m。標準 +4 / タイル（踏面 0.29 m） |
| 橋 | `bridge` 矩形。床板と橋脚、下は埋めない。高さの二重（隧道の上の高速）は持たず、高架は `overpass` のまま |

見送った案: 半タイル解像度（階段・坂は生成で刻めるので不要）、16 bit PNG（38 m で足りる）、負の高さ（空堀・川は周りを上げる）、
2 チャンネル（R = 高さ、G = 種別。塗り絵として確認できないので、種別は JSON の矩形にした）。

資料との食い違い（報告のみ、資料は直さない）:

- F08 の石段「約 200 段 ≒ 30 m」に対し、地図の石段は x 8〜9・y 3〜16 の 14 タイル（16 m）。標準の +4 / タイルなら 56 段・8.4 m。
  200 段を地図の長さに収めると 1 タイル 14 段（踏面 8 cm）になり歩けない。**残りの段は F07 側と画面外の登りとして扱い、F08 の中は 56 段前後にする**つもり。
- F03 の「法面の階段」は南の出入口 (8,27) へ下る x 8〜9・y 23〜27 の 5 タイル。高低差は資料に無いので、標準 +4 / タイルで 20 段・3 m にする。

## T-2b 地形の実装（F03 から）

`docs/FIELD_FORMAT.md` 第 8 節が実装後の形。コード: `field_data.gd`（読み込み・段差の当たり・4 隅・`ground_y`・検証）、`field_builder.gd`（`_build_terrain`: 平らなタイルの行、坂、階段、段差の面、裾、橋、外周）、
`field_scene.gd`（足元の高さ、`_can_enter`、注視点の追従、地形の面の診断）、`minimap.gd`（段差の線・階段）、`building_gen.gd`（`add_quad4`）、`pixel_textures.gd`（擁壁・石垣・岩・土・踏面・水田）。

決めたこと:

| 項目 | 決めた値 | 理由 |
| --- | --- | --- |
| 登れる段差 | 0〜1 階調（0.15 m）。2 以上は坂・階段の中だけ | 承認時の指示（またぐ動きは絵が軽い） |
| 階段の縁 | 進行方向の隣だけで決める。幅方向は一定 | 最初の規則（周りの平らなタイルの平均）だと F09 の主郭の段が脇の曲輪に引きずられ、上端が 0.4 m 下がった |
| 坂の縁 | 周りの平らなタイルの平均。坂・階段どうしは続き | F03 の法面と階段が隣り合う縁に段差を出さない |
| 裾 | 坂・階段の縁が隣の平らなタイルと合わないとき垂直の面を自動で立てる | F08 の石段の脇（山の斜面）、F16 の石段の脇（岩壁）が抜けて見えた |
| 矩形の優先 | 階段 > 坂 > 橋 > 壁 | F09 で壁のテクスチャの矩形が坂・階段を覆って奪った |
| 地形のフェード | 3 階調以上の段差の面は建物と同じ α 0.35 | F03 の隧道で高架の台の壁が主人公を隠した。診断値 `terrain_occl_frames` も残す |
| AO | 3 階調以上の面は遮蔽物（薄い箱）。坂は平行四辺形で近似して焼く | 擁壁の根元が暗くなる |
| 検証 | 高さ画像の厳密さと大きさ、矩形の範囲と kind、階段の単調性、段差で分断された歩けるタイル、坂の傾き（8 階調 / タイル超は助言）、`when` 付きの出入口・調べ物は条件が無ければ到達を求めない | |
| 生成器 | `tools/fieldgen.py`（共通）、`tools/gen_fields_a.py`（A 群）、`tools/gen_fields_b.py`（10 枚）、`tools/gen_placeholders.py`（木・岩・塔） | |

F03 の最初の 3 枚（バス停・法面の階段・隧道）は `t2b_f03_*.png` は残さず、T-3 の 1 枚に含める。

## T-3 残り 10 枚（屋外のみ）

順番: F07、F14（地形なし）→ F11（屋外のみ）→ F03、F10、F15（段差が小さい）→ F04、F08、F09、F16（高低差が大きい）。
資料は `docs/field/<ID>_spec.md` と `refs/field/<ID>.png`、マスク先行、調べ物・出入口は資料の座標、素材はプレースホルダ、数値目標なし。
資料との食い違いは `docs/field/DEVIATIONS.md`。報告画像は `t3_<id>.png`（夕方 1 枚 + ミニマップ）、16 枚の一覧は `fields_16.png`。

### 結果（夕方 1 枚 + ミニマップ: `t3_<id>.png`、一覧: `fields_16.png`）

診断値は記録のみ（目標にしない）。自動歩行は最初の出入口から最後の出入口へ（出入口が 1 つのフィールドは最も遠い歩けるタイルへ）。

| フィールド | 大きさ | 高さ | 地形の矩形 | 自動歩行 | フェードのフレーム | うち地形の面 |
| --- | --- | --- | --- | --- | --- | --- |
| F07 光明院 門前 | 48×36 | 石段の登り口 1.8 m | 階段 1 | 39 m / 997 f | 171 | 0 |
| F14 朝和の里 | 56×36 | 平坦 | 0 | 47 m / 1428 f | 0 | 0 |
| F11 磐戸第一小学校 | 48×40 | 平坦 | 0 | 44 m / 1362 f | 717 | 0 |
| F03 バスストップ・高架下 | 48×28 | 道 3.0 / 高速 9.0 / 法面の下 0 | 階段 1、坂 1 | 12 m / 329 f | 0 | 0 |
| F10 運動広場・河川敷 | 48×32 | 堤防 3.0 | 坂 2 | 33 m / 803 f | 0 | 0 |
| F15 河畔・御渡橋 | 64×24 | 川面 0 / 河原 1.2 / 堤防・橋 3.6 | 階段 2、坂 1、橋 1 | 55 m / 1122 f | 0 | 0 |
| F04 八柱の谷戸 | 48×32 | 西 0.6 → 東 3.6、南の崖 | 坂 1 | 56 m / 1522 f | 158 | 0 |
| F08 臥牛山 天神社 | 40×48 | 麓 0 / 山上 8.4 | 階段 1（56 段） | 55 m / 2250 f | 0 | 0 |
| F09 磐戸城址 | 56×44 | 空堀 0 / 地面 3.0 / 曲輪 3.6 / 主郭 4.8 / 土塁 6.0 | 階段 1、坂 6、壁 3 | 62 m / 2034 f | 1242 | 1242 |
| F16 薬師谷 | 32×48 | 林道 0 / 谷底 4.8 / 奥 6.0 / 岩壁 6.6〜9.6 | 階段 1、坂 1 | 44 m / 1194 f | 554 | 415 |

フレーム時間は全フィールドで平均 16.7 ms（60 fps 上限）、最大 40 ms（F07 の最初のフレーム）。AO の焼き直しは 0.4〜3.4 秒。

### 地形の仕組みで分かったこと

- **形式は足りた。** 10 枚のうち 8 枚に高さがあり、階段 7、坂 12、橋 1、壁 3 の矩形で足りた。高さの二重（隧道の上の高速、橋の下の河原）だけは表せず、高架は `overpass`、河原は橋で分けた（DEVIATIONS.md）。
- **登れる段差 1 階調は詰まらなかった。** 検証の「段差で分断」は、坂・階段を明示すれば 0 になる。F15 の 30 タイルはバリケード（配置物）で塞いだ橋の先で、段差ではない。
- **階段の縁は進行方向の隣だけで決める**のが正しかった。周りの平均だと脇のタイル（曲輪・境内）に引きずられて段の上端が下がる。脇は段々でも壁でもよく、裾が自動で塞ぐ。
- **矩形の優先順位**（階段 > 坂 > 橋 > 壁）が要る。壁のテクスチャの矩形を大きく描くと坂・階段を奪う。
- **カメラ側の高い地形は建物と同じ問題を起こす。** F03 の隧道（高架の台の壁）、F16 の谷底（南西の岩壁）で主人公が隠れた。3 階調以上の面と 0.45 m より高い地形の上面を建物と同じ α 0.35 でフェードし、地形は足元（0.2 m）も判定に入れた。
  ただし**暗い主人公は明るい面のフェードに溶ける**（F16 の岩壁の上面）。フェードは万能ではなく、カメラ側の地形は低くする（F16 の西の縁 1.8 m）のが先。
- **自動歩行は出入口が 1 つのフィールドで止まっていた**（経路が空）。最も遠い歩けるタイルへ歩くようにした。
- **斜面のちらつき**は坂の面が少なく等速パンの計測をしていない。石段の踏面は `stone_step`（目地の明暗を抑えた版）、擁壁・石垣も T-1b の規則（明暗差を抑える）で作った。斜面の MAD は素材が入ってから DOF OFF で測る。
- 生成器の落とし穴: 上書きのとき前の模様（水田）が残る（`fill` で消すようにした）。建物の区画は歩けるタイルを塞ぐ（F11 の旧校舎が東門を塞いだ、F04 の小屋が獣道を塞いだ）。
