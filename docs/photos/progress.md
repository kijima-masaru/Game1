# 参考資料収集 進捗記録

依頼: 「参考資料の収集と寸法語彙の作成（エージェントチーム）」
期限: 2026-09-07 08:00 JST

## 状態（2026-09-06 時点）

**A・B チーム（写真収集）: 実行不可（ブロック済み）。C チーム（寸法語彙）: 実施中。**

### なぜ写真収集ができないか

このセッションが動くサンドボックスの egress プロキシは、npm/pypi/crates/
GitHub/Anthropic API など限られたホストしか外部通信を許可しない組織ポリ
シーになっています。検証結果:

| 手段 | 結果 |
|---|---|
| `curl` で commons.wikimedia.org, example.com, via.placeholder.com 等へ接続 | プロキシが CONNECT を 403 で拒否 |
| `curl` で `raw.githubusercontent.com` へ接続 | **成功する**（GitHub 関連ドメインは許可リストに入っている。git 操作用と思われる） |
| `WebFetch`（任意 URL の取得） | 一般サイトは `EGRESS_BLOCKED` エラー（curl と同じポリシー） |
| `WebSearch` | テキストスニペットと URL のみ。画像本体は取得できない仕様 |
| Google Drive コネクタ | 接続済みだが関連写真資料なし |
| GitHub（このセッションのスコープ） | `kijima-masaru/game1` 単体に限定。他リポジトリの検索/横断は指示で禁止 |

**訂正（2026-09-06）**: 当初「curl は一切使えない」と記録したが、再検証で
`raw.githubusercontent.com` など GitHub 関連ドメインへは接続できることを
確認した。ただし実在の町並み・建物写真が GitHub 上に置かれているケースは
実質皆無で、それを探すための GitHub 全体の横断検索もリポジトリスコープの
制約で行えない。したがって**結論（実写真は収集できない）自体は変わらない**。

つまり、実写真を 1 枚もダウンロードできない状態です。収集枚数は
**フィールド別・カテゴリ別すべて 0 枚**。`refs/photos/` の下は空のディレ
クトリ構成のみ用意しました（詳細は `refs/photos/README.md`）。

### 方針転換

写真が無い前提で、C（寸法語彙）に全力を投入。
- シード表（C-1）はそのまま採用
- 建築モジュール・道路構造物・街路設備・社寺建築の標準寸法を WebSearch
  でテキストベースに調査し補強（事実・数値のみの抽出。写真は使わない）
- 経年（雨だれ・苔・錆・退色・ひび）は、実写真からの実測ではなく、
  建築劣化に関する一般的な知見（どこから発生し、どちら向きの面に出るか
  という定性的パターン）として記録し、「推定・要写真検証」と明記
- 現在の建物生成器（`scripts/field/field_layout.gd`,
  `scripts/field/field_builder.gd`, `scripts/town/building_gen.gd`）の
  実装値を確認し、寸法語彙とのギャップを具体的に指摘（例: `OVERHANG_M`
  = 0.5m。シードの推奨 600〜900mm の下限未満）

### 成果物

- `docs/REFERENCE_VOCAB.md` — 主成果物
- `refs/photos/README.md` — 収集不可の理由と再開方法
- `refs/photos/index.csv` — ヘッダー行のみ（収集物なし）
- 本ファイル

### 再開時に読むもの

1. 本ファイル（`docs/photos/progress.md`）
2. `refs/photos/index.csv`（追記があれば）
3. `docs/REFERENCE_VOCAB.md` の各項目の「出典」欄
   （`不明（裁量値）` の項目が要検証。特に 3-5節・6節・7節）

## 取得済み URL のハッシュ一覧

写真取得が発生していないため、なし。

## 完了記録（2026-09-06）

C チーム（寸法語彙）の作業を完了した。並行して 5 テーマの WebSearch 調査
エージェントを実施し、結果を `docs/REFERENCE_VOCAB.md` に統合済み:

1. 日本建築モジュール寸法（軒の出・屋根勾配・破風・雨樋・基礎・窓・玄関・
   換気口類・看板・電線引込み・物干し）
2. 道路・街路設備の規格寸法（車道・歩道・側溝・電柱・街灯・白線・ガード
   レール・マンホール・カーブミラー）
3. 塀・フェンス規格寸法（ブロック塀・生垣・金網フェンス・木塀、建築基準
   法施行令の控え壁規定）
4. 社寺建築の各部寸法比率（鳥居・石段・灯籠・手水舎・築地塀・山門）
5. 建築劣化パターンの文献調査（雨だれ・苔・錆・退色・ひび割れ・補修跡の
   発生位置と方位依存）

`docs/REFERENCE_VOCAB.md` に「出典」欄を付けて、シード値／文献補強／不明
（裁量値）の 3 区分を明記した。写真による実測は依然として 0 件。

**本タスクはこれで完了とする。** 追加の写真収集手段が無い限り、これ以上
続けても内容が増えない（テキスト調査で見つかる出典はすでに調査済み）。
残作業は依頼者による現地撮影（`docs/REFERENCE_VOCAB.md` 10節の仕様）のみ。
